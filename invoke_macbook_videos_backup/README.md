# invoke_macbook_videos_backup

Script creates / re-creates a new folder under `~/Downloads` called `videos` and copies all movie files to that folder from the macbook `Photos Library`, `originals` folder